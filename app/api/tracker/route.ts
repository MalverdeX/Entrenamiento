import {and,desc,eq} from "drizzle-orm";import {getDb} from "../../../db";import {entries,members} from "../../../db/schema";
const cleanGroup=(v:unknown)=>String(v??"").trim().toLowerCase().replace(/[^a-z0-9-]/g,"-").replace(/-+/g,"-").slice(0,48);
export async function GET(request:Request){try{const group=cleanGroup(new URL(request.url).searchParams.get("group"));if(group.length<3)return Response.json({error:"Código de grupo inválido."},{status:400});const db=getDb();const[people,logs]=await Promise.all([db.select().from(members).where(eq(members.groupCode,group)).orderBy(members.name),db.select().from(entries).where(eq(entries.groupCode,group)).orderBy(desc(entries.loggedAt),desc(entries.id)).limit(500)]);return Response.json({members:people,entries:logs})}catch(error){console.error(error);return Response.json({error:"El registro compartido no está disponible por ahora."},{status:500})}}
export async function POST(request:Request){try{const p=await request.json() as Record<string,unknown>;const group=cleanGroup(p.group);if(group.length<3)return Response.json({error:"Código de grupo inválido."},{status:400});const db=getDb();if(p.action==="member"){const name=String(p.name??"").trim().slice(0,40);if(!name)return Response.json({error:"Escribe un nombre."},{status:400});const[existing]=await db.select().from(members).where(and(eq(members.groupCode,group),eq(members.name,name))).limit(1);if(existing)return Response.json({member:existing});const[member]=await db.insert(members).values({groupCode:group,name,createdAt:new Date().toISOString()}).returning();return Response.json({member},{status:201})}if(p.action==="entry"){const memberId=Number(p.memberId),dayId=Number(p.dayId),exercise=String(p.exercise??"").trim().slice(0,100);const[owner]=await db.select().from(members).where(and(eq(members.id,memberId),eq(members.groupCode,group))).limit(1);if(!owner||!exercise)return Response.json({error:"Datos de entrenamiento inválidos."},{status:400});const rawWeight=String(p.weight??"").replace(",",".");const weight=rawWeight===""?null:Number(rawWeight);if(weight!==null&&(!Number.isFinite(weight)||weight<0||weight>1000))return Response.json({error:"Revisa el peso ingresado."},{status:400});const[entry]=await db.insert(entries).values({groupCode:group,memberId,dayId,exercise,weight,reps:String(p.reps??"").trim().slice(0,60),note:String(p.note??"").trim().slice(0,160),loggedAt:new Date().toISOString()}).returning();return Response.json({entry},{status:201})}return Response.json({error:"Acción no válida."},{status:400})}catch(error){console.error(error);return Response.json({error:"No pudimos guardar. Tus datos siguen en pantalla; intenta otra vez."},{status:500})}}
